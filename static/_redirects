# Redirect from .pages.dev to custom domain
https://jakes-tech-insights.pages.dev/* https://jakeinsight.com/:splat 301!

# Japanese language routes (removed)
/ja/categories/* / 301
/ja/tags/* / 301
/ja/ / 301
/ja/* / 301

# /en/ prefix → actual paths (EN content has no /en/ prefix in Hugo output)
/en/ / 301
/en/all-posts/ /all-posts/ 301
/en/all-posts/* /all-posts/:splat 301
/en/tags/* /tags/:splat 301
/en/categories/* /categories/ 301

# /en/* prefix-removal: dev-prefixed URLs (slug already has date prefix → maps
# cleanly to the new section/slug). Older slug-only URLs land at 404, which is
# the right signal for Google to drop them from the index.
/en/side-income/* /side-income/:splat 301
/en/side-income/ /side-income/ 301

# /tech/ section was retired on 2026-06-03 (posts migrated to /ai|productivity|
# tech-economy|buying-guide/; section _index.md removed). The 837 specific
# /tech/<slug>/ → /<new-category>/<slug>/ rules are auto-appended below by
# migrate_categories.py. The catch-alls here handle the bare section URL and
# any /en/tech/* leftovers.
/tech/ /ai/ 301
/en/tech/ /ai/ 301
/en/tech/* /ai/:splat 301
/ko/tech/ /ko/ai/ 301

# /categories/tech/ taxonomy page is no longer built (no posts in "tech" since
# the migration). Send legacy inbound links to /categories/ which lists all
# active taxonomies.
/categories/tech/ /categories/ 301
/categories/tech /categories/ 301
/ko/categories/tech/ /ko/categories/ 301

# Legacy URLs are intentionally NOT listed — they fall through to Cloudflare's
# default 404, which is sufficient to remove them from Google's index.
# (Cloudflare Pages _redirects does not support status 410, only 200/301/302/303/307/308.)
#
# Removed legacy patterns (now 404):
#   /technologies/*, /tags/mini/
#   /sports/*, /business/*, /entertainment/*, /society/*, /finance/*
#   /ko/sports/*, /ko/business/*, /ko/entertainment/*, /ko/society/*, /ko/finance/*
#   /categories/business/, /categories/entertainment/, /categories/sports/, etc.
#   Old undated tech posts

# Auto-generated by migrate_categories.py — old URLs → new category routes
